# Artist Discovery RAG — pinned dependencies
# Regenerate with: pip freeze > requirements.txt

# Web UI
streamlit==1.32.0

# LangChain (LCEL stack — no legacy langchain.chains)
langchain-core==1.3.3
langchain-anthropic==0.3.22
langchain-chroma==1.1.0

# Anthropic SDK (used transitively by langchain-anthropic)
anthropic==0.99.0

# Vector store
chromadb==1.5.9

# Embeddings (local, no API call)
sentence-transformers==2.3.1

# Data fetch
requests==2.33.1
rich==13.5.3

# Environment
python-dotenv==1.0.0
